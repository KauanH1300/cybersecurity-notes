# ⚙️ CPU - Unidade Central de Processamento

A CPU (Central Processing Unit) é o componente responsável por executar instruções e coordenar as operações do computador.  
Tudo o que um programa faz, desde uma soma simples até rodar um sistema operacional, passa pela CPU.

---
## 🧠 O que é a CPU

De forma simbles a CPU é um chip responsável pelo processamento de dados em um computador, celular e outros dispositivos eletrônicos, funcionando como o “cérebro” desses equipamentos. Ela recebe instruções, interpreta o que precisa ser feito e executa essas instruções seguindo uma sequência bem definida.
Embora pareça que ela é uma parte única do equipamento, isso não é verdade. Na realidade a CPU é um conjunto de diferentes componentes de um computador que trabalham juntos de forma altamente orquestrada.

---
## ⚙️ Quais são os componentes de uma CPU

### 🔹Unidade de Controle (UC)
Responsável por coordenar o funcionamento da CPU:
- busca instruções na memória
- decodifica o que deve ser feito
- controla o fluxo de dados entre os componentes
### 🔹Unidade Lógica e Aritmética (ULA)
É onde acontecem as operações:
- cálculos matemáticos (soma, subtração, etc.)
- operações lógicas (AND, OR, NOT, comparações)
### 🔹Registradores
São pequenas áreas de memória **dentro da CPU**, extremamente rápidas.

Servem para:
- armazenar dados temporários
- guardar endereços de memória
- controlar o fluxo de execução
